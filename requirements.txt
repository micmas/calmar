# Python dependencies for the LINDA notebook + aphasia-kb tooling.
# Install on a new machine with:
#     pip install -r requirements.txt
#
# (Most of these come pre-installed in Neurodesk; this file is the
# reproducibility contract.)

# Core neuroimaging
nibabel>=5.0
nilearn>=0.10
scipy>=1.10
numpy>=1.24
pandas>=2.0

# Notebook UI
ipywidgets>=8.0
ipyniivue>=2.0
matplotlib>=3.7

# DICOM / extras used by the original template cells
pydicom

# Knowledge-base loader + annotator
PyYAML>=6.0
pymupdf>=1.23           # for annotate_paper.py (PDF highlight annotations)
                        # AND extract.py (PDF text extraction)

# CLI extraction (aphasia-kb/extract.py)
anthropic>=0.40         # Anthropic Python SDK; needs ANTHROPIC_API_KEY env var

# Notebook validation (used by the workflow)
nbformat>=5.0

# NiMARE — coordinate-based meta-analysis (Stage-2 Neurosynth
# bootstrap + KB-internal meta-analysis). Promoted from optional to
# required on 2026-05-04 so a single `pip install -r requirements.txt`
# yields a working install on every machine.
#
# NiMARE pulls in scikit-learn, statsmodels, indexed-gzip, tqdm,
# fuzzywuzzy, requests, etc. We pin the umbrella package; pip resolves
# the rest. We also pin scikit-learn / statsmodels explicitly so a
# stale Neurodesk pre-install doesn't get held back.
nimare>=0.5.0
scikit-learn>=1.3
statsmodels>=0.14

# Optional: dataset management. Comes pre-installed in Neurodesk; if
# you're running this elsewhere you'll need datalad too:
# datalad>=0.19
